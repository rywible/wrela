[workspace]
resolver = "3"
members = [
    "compiler",
    "runtime",
]

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
panic = "abort"

[profile.dev]
debug = 0
split-debuginfo = "off"
incremental = true

[profile.test]
debug = 0
split-debuginfo = "off"
incremental = true
