[package]
name = "wrela_runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["staticlib", "rlib"]

[features]
default = ["metrics"]
metrics = []

[dependencies]

[dev-dependencies]
serde_json = "1.0"
tempfile = "3.10"
