[package]
name = "wrela_runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["staticlib", "rlib"]

[features]
default = ["metrics"]
metrics = []
abi_typed_fast_path = []

[dependencies]
libc = "0.2"

[dev-dependencies]
serde_json = "1.0"
tempfile = "3.10"
