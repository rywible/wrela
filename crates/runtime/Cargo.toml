[package]
name = "wrela_runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
tokio = { version = "1.48", features = ["rt-multi-thread", "sync", "time"] }
