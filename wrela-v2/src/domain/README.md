# domain

Pure language/runtime invariants and policy-level logic live here in v2.

Platform-neutral contracts and models live under `domain/platform/`.
No OS-specific primitives are allowed in this layer.
