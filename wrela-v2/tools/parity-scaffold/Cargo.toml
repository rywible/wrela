[package]
name = "wrela_v2_scaffold"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Scaffold for Wrela v2 architecture and parity interfaces"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3.12.0"
