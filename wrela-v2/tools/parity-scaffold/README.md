# Parity Scaffold Crate

This crate hosts Rust-side parity/contract placeholder interfaces for early V2 readiness.

It is intentionally isolated under `wrela-v2/tools/` so the V2 root directory remains
reserved for architecture-first language/runtime layout.
