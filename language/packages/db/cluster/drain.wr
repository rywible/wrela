//
// Region draining orchestration contract.

class DrainPlan {
    region: String
    strict: Boolean

}
fn drain_region(region: String) -> DrainPlan {
    return DrainPlan(region=region, strict=true)
}
