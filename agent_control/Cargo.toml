[package]
name = "wrela_agent_control"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Deterministic agent control foundation for Wrela"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
