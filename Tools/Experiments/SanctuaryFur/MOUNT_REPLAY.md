# Current mount anatomy and attachment review boundary

No0130 established native mount image was available at preparation time. The
established-route runtime contained a coast-edit capture only. No new anatomy
defect or geometry change is inferred from missing images. This is a prepared,
unexecuted20-capture source study, using current registered defaults:

```sh
export WRELA_CONTROL_ROOT=/absolute/matching/workspace/.soundstage
./scripts/stagectl replay Tools/Experiments/SanctuaryFur/mount-acceptance.json > "$WRELA_CONTROL_ROOT/mount-current-receipt.json"
```

The recipe explicitly selects Sanctuary/catalog, Moonhart then CloudRay, uses
the shared softbox and material look with no exposure override, default source
controls, dry surfaces, scale1 and seed17. It includes front/quarter/left bind,
Moonhart side hop at0,.187,.3995,.612,.731,.85s; CloudRay side flight at
0,2.18,4.36,6.54s; and two front idle samples per species. Predicted closed-eye
times derive from the production blink hash, including Ray's+0.8s offset.
Inspect actual lid state; labels do not certify a blink. The recipe resets the
physical camera after the creature command to avoid auto-follow changing the
requested distance: Moonhart3.4m,CloudRay4.2m,FOV60.1605682°. Require full body,
antlers/wings/tail in frame and retain exact actual framing for subsequent pairs.

Specific comparisons:

- Front/quarter: dark pupils and focused gaze after the shared normal repair;
  seated eyes rather than detached rings; muzzle/cheek/neck continuity.
- Moonhart left bind/hop: hip/shoulder and knee overlap through flexion, hoof
  orientation, anticipation and landing. Hop apex is intentionally airborne.
  Inspect actual contacts separately from dark contact shadows.
- Ray left flight: mantle-to-tail root continuity, eyelid attachment during
  bend, continuous wing surface and unbroken outline. Studio support clearance
  is independent of the player's mounted attachment.
- Front idle: eyelids, ears and gaze remain readable without detached parts.

The studio hop is the shared inspection clip, **not** the saved accepted-distance
mounted gait. Actual mounted review needs accepted movement, stop/restart,
turning and reopen from the established native journey. No independent clock
gait or camera change is added by this study.

Sky owns `SanctuaryMountAttachment` and common placement. Its current flying
eye bind is(0,.95,+.25)×morphologyScale plus.08m vertical clearance; root yaw is
−cameraYaw. Sky reported five actual compiled/skinned central-cone tests passing
in0120 across four riding/two flying species and retained negative control.
That is numeric clearance evidence, not actual0130 visual acceptance. Native
views at pitch0,−.08,−.30 must expose central terrain while retaining useful
lower-body/wing cues. Any attachment defect is handed to sky; this worker owns
only evidenced mount geometry/motion repairs. Undercoat remains default off.

Prepared JSON syntax and current undercoat source hashes were checked with a
small filesystem script. No Swift test, build, app, GPU or native capture was
run by this worker. Native image and source-fingerprint receipt remain pending.

## Actual0130 receipt received later

Inspected both frame15 and ordinary4Down captures. At pitch−.08 a lower head
cue remains; at−.38 the head rises to about y645 of1080 with broad lower-corner
wing cues. Both image centers remain outside the animal silhouette; nearby
palm foliage is a separate obstruction. Plain round head, flat brown mantle
and protruding left-eye silhouette remain art limitations, not demonstrated
root gaps. These show CanopyGlider, so they do not justify speculative changes
to owned Moonhart/CloudRay anatomy. Exact PNG/metadata hashes and limits:
`.build/sanctuary-native-20260912/groom-0130-review/mount-native-readability.json`.
No model, exposure or attachment change was made.
