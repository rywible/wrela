# 352-strand acceptance against actual 0017 evidence

## Actual0212 native rejection

All30 matching PNGs inspected: rectangular raised tiles/basket pattern remains
over smooth skin, with stronger wet plastic highlights. Face/lining and sampled
parent motion remain readable, but fur identity fails. Default stays off; no cost
profile or baseline promotion. Hash archive:
`.build/sanctuary-native-20260912/undercoat-0212-review/review.json`.
Next diagnostic and exact scalar replay are in
`Tools/Experiments/SanctuaryFur/COVERAGE_DIAGNOSIS.md`; no geometry revision follows
from the CPU pass.


## Actual0212 compiled qualification

The current adaptive source passed all four production coat tests in32.455s;
the focused run passed21 tests with zero failures. Actual default retained-vertex
exposure is0.79492533, maximum sampled reduction error0.0009913478m, raw mesh
bytes484,448. All three owned source/test hashes exactly match immutable0212
and the checkpoint. Receipt, copied source and log SHA are retained in
`.build/sanctuary-native-20260912/undercoat-0212-qualification/compiled-receipt.json`.
This supersedes earlier uncompiled/estimated-count status below. Exact adaptive
vertex/triangle counts were not printed; do not infer them from raw bytes. Native
images, material identity and live cost remain unqualified; default remains0.


Status: all four actual compiler tests pass on immutable0130, but all30 native
images reject its fur identity. The next conformal-undercoat revision is
uncompiled and unrendered. Default groom remains off. The0130 strand criteria
below are retained history; the new patch contract is described here.

The candidate replaces tubes with352 overlapping conformal patches,220 body /
112 head /10 each outer ear. All four parents, root/exclusion/contact/pose
limits and16,896-triangle /1MiB /four-recipe caps remain. Each patch has7×5
vertices and follows the authoritative parent field across its full width;
its entire root edge is buried1.5mm. Body patches span55×67mm, head35×43mm,
ears13×23mm; height is millimetric rather than the previous centimetric curl.
Unsafe face/lining/low-body candidate patches are explicitly excluded before
selection; exact regional counts are required, so missing coverage cannot
silently return fewer patches. Source projection/normal/accuracy failures throw.

Existing `GroomCoverage` and its native `hasGroomCoverage` path integrate
0.8mm strand periods with0.94 root duty and0.18 tip-length variation. This is
an unresolved fibre coverage approximation, not volumetric fur scattering.
No shared shader/ABI or roughness change is introduced. Tests now use one root
edge per patch and sample the actual production coverage function at interior
and tip. The1mm accuracy guard compares denser25×9 projected source samples
against the actual7×5 triangular interpolation, in both surface directions.
Predicted unchanged topology12,320 vertices /16,896 triangles /991,232 bytes
needs a new compiled receipt;0130 metrics do not certify these new patches.

Same native replay is required. Reject broad visible patch borders, roof-tile
or feather/scale reading, streaks, transparency holes, plastic highlights,
silhouette popping or still-bare clay areas. Retain clear eyes/muzzle/lining.
No new native game promotion or cost run until isolated fur identity is useful.

Definitive CPU receipt: `.build/validation-candidates/sanctuary-ear-groom-20260913-0130/.build/candidate-stage-logs/`
contains the exact `focused-swift-tests.log` indexed and SHA256-hashed by
`groom-source-checkpoint.json`. Four tests passed with zero failures in19.535s:
actual roots/exclusions6.668s, animated parents0.047s, semantic corners/cache
9.617s, registered source/base parity3.202s. Default compiled exposure is
0.71428573, maximum reduction deviation0.0006238807m and raw mesh bytes991,232.
Workspace coat/design/test source hashes match the immutable copied files.
Root retains124 unchanged-source passing tests with an explicit manifest delta;
those are reused evidence, not part of this four-test run. The failure history
below explains the source repairs and does not supersede this successful gate.

0120 CPU receipt: all four coat tests rejected the fixed-ring reduction because
it exceeded1mm; the other124 tests passed per root. The bounded source repair
chooses exactly six of the actual25 source rings using a deterministic minimax
partition, considering all five vertices per ring. Catmull-Rom guide curvature,
strand length variation and transported frames make uniform ring selection
unreliable. The triangle/storage caps and1mm limit are unchanged. The repaired
source still needs root's actual compiler tests and matching images; it throws
with exact strand/rings/error if the optimal six-ring partition is insufficient.

0124 confirmed the best six-ring partition still exceeded the limit: strand40,1.0156704mm, rings[0,3,10,13,19,24]. Root authorized a modest triangle increase while retaining the1MiB/4MiB limits. Current source therefore keeps the original guide curve and selects seven rings (six spans),12,320 vertices,16,896 triangles and991,232 raw bytes. The1mm gate is unchanged; all semantic corners still require the next real compiler run. A transient middle-control edit was superseded and fully restored before the final source boundary. Native incremental cost remains unmeasured.

0126 actual default receipt:991,232 raw bytes, exposure fraction0.71428573 and maximum fine-to-retained deviation0.6238807mm. Default attachment, animated-parent and registered/base parity tests passed; the semantic-corner test rejected an ear-lining intrusion. This corrects the earlier hand estimate1,013,760 bytes. The next narrow source correction keeps six seats per ear but selects broad back-surface triangles (back distance>26mm, normal.z>0.8), preserving the strict compiled6mm lining guard. It requires rerunning all four affected tests; these0126 measurements do not qualify the corrected ear seats.

## Evidence and precise failure

All30 original captures are indexed with source/image hashes in
`.build/sanctuary-native-20260912/groom-0017-review/review.json`; the native receipt
is `groom-parity-0017-stage-replay.json` in the same native root. Reinspected these
actual PNGs in `groom-parity-0017-stage-held/.soundstage/captures` for this gate:

- `groom-1-quarter-wet0-cd882644.png`: sparse dark marks on crown/haunch; the
  overall coat remains smooth clay. Dark eyes and muzzle remain unobstructed.
- `groom-1-left-wet1-a116dc61.png`: smooth broad highlights dominate; minute
  strands do not establish a wet furry surface. The existing neck/limb creases
  remain visible, with no new open groom attachment seam discernible.
- `groom-1-hop-03995-140e3c08.png`: actual time0.400000125s, grounded0;
  intentional airborne paws. Marks follow the raised head/body.
- `groom-1-hop-0731-e0d3c350.png`: actual time0.733333290s, grounded1;
  landed pose. A soft/dark floor shadow is not a measurement of sole penetration.

Both hop metadata files contain `contacts=[]`. This cannot certify planted
contacts. Child coat `jointPositions` describe transformed zero pivots, not
physical strand roots; comparing them with a parent's anatomical pivot would
invent an attachment error. Use actual posed vertices and the production rig.

The original1.4mm root diameter projects to approximately0.573px at the recorded
2.2761991m /60.1605682degree FOV /1080p camera before orientation foreshortening.
Together with sparse/rear-biased source placement, this explains poor visible
coverage. No actual 0017 buried arc-length fraction was dumped, so burial alone
is not established as the main cause.

## Independent hard gates

| Gate | Concrete threshold and evidence |
| --- | --- |
| Base compatibility | Omitted selector and fur0 retain all original mesh indices/positions/normals/colors/groom values; same brain and base joint poses |
| Root attachment | Every compiled root-ring vertex inside the authoritative parent field; its center exits the actual rendered parent mesh along the source normal in less than12mm |
| Face and lining | Every strand vertex has eye/nose/muzzle source-field value greater than8mm; ear roots originate on actual outer-ear triangles and all ear vertices remain behind `pivot.z+6mm` |
| Sole exclusion | Every strand vertex has bindY greater than0.31m; no paw or low-limb groom |
| Parent motion | Every actual strand vertex transformed by its child matrix agrees with its parent matrix within1µm across the registered idle/hop samples |
| Contact preservation | Existing semantic tests: sole minY at bind within0.5mm; all hop soles at or above−0.5mm; planted sole position change below10µm. Airborne phases are exempt from being on the floor, not from finiteness |
| Exposed geometry | More than55% of retained compiled vertices lie greater than1mm outside their own parent field; this is a geometric sample fraction, not visible-pixel coverage or fur identity |
| Reduction | Every omitted fine strand-ring vertex differs from the retained piecewise-linear representation by at most1mm; preserve sampled normals/colors/groom and winding |
| Size and cache | Exactly352 strands, four added batches, at most16,896 triangles and1MiB raw mesh per recipe; at most four recipes /4MiB cached coat. Expected source totals12,320 vertices and991,232 raw bytes; confirm from compiled output |

These criteria are implemented by the updated coat tests or existing semantic
tests, but the updated 352 tests still require root's successful matching run.
Source-field margins are not a universal exact Euclidean clearance guarantee.
The root/parent rendered-mesh intersection check supplies separate attachment
evidence. No extra shadow triangles are expected (`castsShadow=false`).

## Native appearance, exposure and motion gates

Run the existing [matched replay](GROOM_REPLAY.md) with `coatStudy=0` and compare
fur0/1 at front/quarter/left, dry/wet, then all six side-hop times and both blink
samples. Verify actual quantized times,1920×1080, MSAA, physical camera distance,
resolved light transform and source/shader fingerprints. A changed framing,
exposure or light invalidates the A/B; do not brighten the candidate to expose
strands. Preserve the shared look and unmodified face materials.

Appearance must visibly change at the recorded interaction framing, without
magnifying screenshots: soft grouped edge fibres and a coherent coat must read
in quarter and side, with no distracting sparse spikes or dark flecks in front.
Reject plastic wires, a detached halo, uniformly radial quills, obscured eyes,
lined ears filled with hair, new attachment gaps or a smooth wet plastic result.
A source exposure fraction or a pixel-difference percentage cannot substitute
for this image judgment. Inspect close interaction once as well: the1mm source
reduction bound is about0.41px at the main framing but about1.86px at0.5m.

Compare hop side views for sliding roots and new creases; verify the same base
contact pose against fur0, then use actual mesh/rig tests to separate geometry
from shadow appearance. The paused strip cannot qualify live shimmer. A short
live idle/orbit must not introduce persistent crawling sparkles, flashing strand
edges or discontinuous silhouette changes. Wetness currently changes shading,
not strand geometry; do not claim simulated wet clumping or flattening.

## Cost gate and material distinction

After a worthwhile appearance result, root measures the same short warm live A/B
with normal clouds/wind/cache. Provisional increments are GPU p95≤0.20ms and CPU
update/encode p95≤0.05ms. Keep median/p95/max, presented stalls, RSS and Metal
allocations; raw mesh bytes do not include GPU allocation overhead. The earlier
0017 populated eye-level view measured GPU median/p95/max25.576/32.937/37.668ms,
RSS max788.563MiB and thermal state fair: it does not meet1080p60. The earlier
15.199ms p95 downward view used pitch−0.64 versus−0.08 and submitted6.46× fewer
visible triangles. It is not representative eye-level headroom. Source/shaders
and thermal state also differ, so this is not a controlled groom cost comparison.
See `.build/sanctuary-native-20260912/groom-parity-0017-populated-profile-analysis.md`.
The incremental groom gate is only a cost ceiling, not permission to add cost to
an already over-budget scene. Isolated studio success cannot certify populated
play or traversal. A short pair near the threshold needs the reversed order
before a cost conclusion.

Fur sources support fibre-layer response plus silhouette fibres, while a
leathery membrane is a continuous sheet whose thickness/tension/roughness need
separate treatment. The current renderer has no physical membrane-thickness
transport contract. See [the bounded semantic proposal](MATERIAL_INTERFACE_PROPOSAL.md)
for source-grounded consumers and explicitly unsupported future fields. No
membrane material is implemented or approved by this fur experiment.
