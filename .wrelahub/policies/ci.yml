name: default
steps:
  - name: tests
    run: "cargo test"
    timeout_secs: 1800
  - name: fmt
    run: "cargo fmt --check"
    timeout_secs: 300
  - name: clippy
    run: "cargo clippy --workspace --all-targets --all-features"
    timeout_secs: 1800
