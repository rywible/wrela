so:
    Parsing helpers.

so: Parse an Integer from a String.
to parse_integer(text: String) -> Result[Integer]:
    return __wr_parse_int(text)

so: Parse a Float from a String.
to parse_float(text: String) -> Result[Float]:
    return __wr_parse_float(text)
