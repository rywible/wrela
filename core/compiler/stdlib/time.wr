so:
    Time helpers.

so: Return a monotonic clock value in nanoseconds.
to get_current_nanoseconds_since_epoch() -> Integer:
    return __wr_clock_ns()

so: Sleep for the given duration in milliseconds.
to sleep(milliseconds: Integer) -> Pending[Nothing]:
    return __wr_sleep_ms(ms)
