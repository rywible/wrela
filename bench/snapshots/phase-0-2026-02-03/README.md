Phase 0 snapshot
Date: 2026-02-03

Notes:
- This repo state predates the Phase 1 microbench harness, so the Phase 0 worktree could not run `tests::microbench_hot_paths_smoke` (0 tests matched).
- `perf.json` is copied from `bench/baselines/perf.json` (the perf gate baseline created during Phase 0).
- `microbench.txt` contains the attempted Phase 0 run output for traceability.
