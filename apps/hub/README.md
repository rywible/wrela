# Wrela Hub Service

This is the Wrela Hub application built on Wrela runtime primitives.

Modules live in apps/hub/src/modules and map to major product areas (auth, governance, repo, packages, rfc, spec, messaging, moderation, ci, audit, search, files, api).
